const assert = require('chai').assert,
    { Entry } = require('factom'),
    { parseVote } = require('../src/read-vote/parse-vote');

describe('Parse vote', function () {

    it('Should parse vote', function () {

    });
});